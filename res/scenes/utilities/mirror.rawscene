# Demo for layering system - shows off render-to-texture capability of layer
# By specifying that a layer should render to texture, this allows you to create a mirror 
# by using a mesh that references the texture. 
# (not yet implemented)

# Needs to be driven off of a layering system, but how would it work? 

# Maybe?
# ^texturerender:position:0 0 0 
# ^targettexture:target:reflection

# someelement:mesh:./res/models/box.obj
# someelement:texture:reflection # overrides for texture?