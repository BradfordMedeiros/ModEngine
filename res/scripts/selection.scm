
(define (onObjSelected id color)
  (display "on object selected: ")
  (display id)
  (display "\n")
)

(define (onObjHover id)
  (display "on object hover: ")
  (display id)
  (display "\n")
)

(define (onObjUnhover id)
  (display "on object unhover: ")
  (display id)
  (display "\n")
)